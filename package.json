{
  "name": "dan-v.github.io",
  "version": "1.0.0",
  "description": "Personal website",
  "private": true,
  "scripts": {
    "build": "@tailwindcss/cli -i ./src/styles/main.css -o ./dist/style.css --minify",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "tailwindcss": "^4.1.5",
    "@tailwindcss/cli": "^4.1.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}